generator client {
  provider        = "prisma-client-js"
  output          = env("PRISMA_CLIENT_OUTPUT")
  previewFeatures = ["postgresqlExtensions"]
}

datasource db {
  provider   = "postgresql"
  url        = env("DB_URL")
  extensions = [citext]
}
